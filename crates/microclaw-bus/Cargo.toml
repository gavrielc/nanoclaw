[package]
name = "microclaw-bus"
version = "0.1.0"
edition = "2021"

[dependencies]
microclaw-protocol = { path = "../microclaw-protocol" }
rusqlite = { version = "0.31", features = ["bundled"] }
serde_json = "1"
